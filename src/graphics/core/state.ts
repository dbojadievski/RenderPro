namespace renderPro {
    export namespace graphics {
        export namespace core {
            export enum State {
                NORMAL = 0,
                TRANSPARENT,
                HIGHLIGHTED,
                HIDDEN
            };
        }
    }
}
